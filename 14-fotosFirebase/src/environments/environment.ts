// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyBMbVQ07OEBfr6SOPQcSkr-GtmWrGGuRpk',
    authDomain: 'backend-login-app.firebaseapp.com',
    databaseURL: 'https://backend-login-app.firebaseio.com',
    projectId: 'backend-login-app',
    storageBucket: 'backend-login-app.appspot.com',
    messagingSenderId: '778259822863',
    appId: '1:778259822863:web:7cff83adce5404afcee67a',
    measurementId: 'G-DSGBSXHPML'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
