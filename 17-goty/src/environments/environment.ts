// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyAiEMMaGv2SV_4TGHNT9ojP6UH1_GrF6MA',
    authDomain: 'goty-project.firebaseapp.com',
    databaseURL: 'https://goty-project.firebaseio.com',
    projectId: 'goty-project',
    storageBucket: 'goty-project.appspot.com',
    messagingSenderId: '709825008001',
    appId: '1:709825008001:web:04e9a387b85cf0d0565880'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
